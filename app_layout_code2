import 'package:flutter/material.dart';

void main() =>
  runApp(MaterialApp(
    home: Home()
));
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FRIEND APP'),
        centerTitle:true,
        backgroundColor:Colors.green,
      ),
     body:Center(
      child:Image(
      image:NetworkImage('https://wallpapercave.com/wp/wp2237255.jpg'),
    ),
     ),
      floatingActionButton: FloatingActionButton(
        onPressed: (){},
        child:Text('ADD'),
        backgroundColor: Colors.blueGrey,
    ),

      );



}
}
